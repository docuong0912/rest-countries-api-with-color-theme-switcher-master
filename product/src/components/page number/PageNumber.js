import React from 'react'

const PageNumber = ({page}) => {
  return (
    <div className='page-list'>
        {Array(25).map(page=>{
            
            return(
                <button>zxc</button>
            );
        })}
        
    </div>
  )
}

export default PageNumber